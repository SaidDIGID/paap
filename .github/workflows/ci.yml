name: CI
'on':
  push:
    branches:
      - main
    paths:
      - "packages/server/**"
      - ".github/workflows/ci.yml"
  pull_request: {}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install
        run: pnpm install
      - name: Test
        run: pnpm test
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install
        run: pnpm install
      - name: Build
        run: pnpm run build
